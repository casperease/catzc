# cspell:ignore btweutstsmplst
# Config for the 'sample' fixture template, base slot of env beta. One config file ⟷ one RG.
# Resource names are written statically here (in-template name generation is intentionally deferred).

ParametersFile:
  parameters:
    storageAccountName:
      value: btweutstsmplst
