{
  "//": "MANAGED ROOT FILE - single source of truth: automation/Catzc.Base.RootConfig/assets/root/dotnet-tools.json, linked to the repo root by Build-RootConfig on every import (the root file IS this file). JSON has no comment syntax and the dotnet manifest parser rejects // line comments, so the marker is this ignored '//' property.",
  "version": 1,
  "isRoot": true,
  "tools": {}
}
