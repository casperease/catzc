# CD pipeline for customer flux — thin root that extends the shared customer CD template.
#
# flux is dev-only (nonprod tier only): one foundation per-sub env (subn), one discovery env (dev), one
# expedition slot (dev-001). Add the prod tier (subp / preprod / prod) when flux grows. All structure
# lives in /pipelines/extends/cd-customer.yaml; triggers live here (honored only at the pipeline root).
# The target subscription is what the service connection logs into; the subscriptionIdAssertIs GUID
# (from azure.yml) pins it.
#
# Reference shape: 'sc-flux-nonprod' is illustrative until real service connections + a non-placeholder
# azure.yml exist.

trigger:
  branches:
    include: [main]
  paths:
    include: [.sha-markers/flux.yml]

pr:
  branches:
    include: [main]
  paths:
    include: [.sha-markers/flux.yml]

extends:
  template: /pipelines/extends/cd-customer.yaml
  parameters:
    Customer: flux
    Foundations:
      - { env: subn, serviceConnection: sc-flux-nonprod, subscriptionIdAssertIs: 00000000-0000-0000-0000-000000000007 }
    Discoveries:
      - { env: dev, serviceConnection: sc-flux-nonprod, subscriptionIdAssertIs: 00000000-0000-0000-0000-000000000007 }
    Expeditions:
      - { env: dev, slot: '001', serviceConnection: sc-flux-nonprod, subscriptionIdAssertIs: 00000000-0000-0000-0000-000000000007 }
